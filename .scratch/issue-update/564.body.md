## Test Definition

Assemble context twice from identical bounded Canon/Character/Party/quest inputs for each role, then repeat with explicitly selected Party/World context, private/cross-role access, and one changed input. Expected: identical versioned snapshots for identical inputs; selected context included; private/cross-role data excluded; changed input invalidates the snapshot/proposal as stale.

## Status

- [ ] Pass
- [ ] Fail

## Learning Outputs

Pending execution; promotion gate is deterministic snapshot equality, least-privilege filtering, explicit context selection, and stale rejection.

## Next Action

If Pass, inspect Epic #554 metric and promote the context envelope; if Fail, refine allowlists, snapshot identity, or budgets.

## Related Work

Parent Epic: #554

## TBP Gap Audit

The canonical experiment fields are present.


## Approved planning update

### Test Definition
Cover authenticated scope input; least-privilege filtering before assembly; stable-ID dedupe; ordering by `(scope_priority, canonical_event_sequence, stable_id)`; World background, Party shared campaign state, and Personal Character-local state; explicit optional empty sections; required-context rejection; bounded conflict result; `snapshot_hash`; `schema_version`; provenance; and byte-equivalent output for identical inputs.

**Learning Outputs:** Pending execution; promotion requires deterministic equality and boundary cases.

**Next Action:** promote only after every assembler case passes.


## Precedence-aware assembly test contract

### Test Definition
Test precedence-aware assembly (server > World > Party > Personal > client, with canonical event sequence then stable proposal ID ordering), same-priority same-field conflict result with no mutation, and bounded error shape containing only `outcome`, `reason_code`, `schema_version`, and a correlation/provenance reference. Assert absence of raw prompts and unrestricted model output.

### Learning Outputs
Confirm lower-priority overwrite rejection, same-priority conflict rejection, bounded error results, and absence of raw prompts/model output.

### Next Action
Promotion requires all precedence-aware assembly cases, same-priority conflict cases, bounded error shape checks, and raw prompt/model output absence checks to pass.


## Version and provenance contract
All proposals carry server-owned `schema_version`, `contract_version`, `prompt_version`, `model_id`, `model_version`, `builder_version`, `tuning_version`, `input_snapshot_hash`, and `provenance_id`. A proposal is stale/invalid when any required version differs from the active contract or when the input snapshot hash no longer matches; the server never silently migrates proposals across versions.

Provenance is bounded to `proposal_id`, `source_role`, `requesting_scope`, `input_snapshot_hash`, schema/contract/prompt/model versions, `canonical_event_sequence`, `outcome`, `reason_code`, `normalized_output_hash`, timestamps, and `correlation_id`. Raw prompts, credentials, private context, and unrestricted generated text are not persisted in provenance. Normalized bounded proposal content is retained separately only after validation and under storage policy.

## Precedence-aware assembly test contract
Test version-pinned snapshots, every stale input/version rejection path, bounded provenance allowlist, and exclusion of raw prompts, credentials, private context, and unrestricted model output. Test same-priority same-field conflicts and verify deterministic precedence-aware assembly, conflict reason codes, normalized output hashes, and correlation/provenance references.

## Test Definition
Use server/world/party/personal/client fixtures with pinned versions and input hashes; assert rejection on any mismatch, no silent migration, bounded provenance only, and validated normalized content stored separately under policy.

## Learning Outputs
Capture bounded outcomes, reason codes, canonical event sequence, normalized output hash, timestamps, and correlation id without raw prompts or private context.

## Next Action
Promotion requires all version-pinned snapshot, stale input/version rejection, same-priority conflict, bounded provenance, and no-raw-prompts/private-context cases to pass.

## Approved Test Definition

Test snapshot TTL expiry and rejection with `STALE_SNAPSHOT`; test exact replay matching for schema, contract, prompt, model, builder, tuning, and input snapshot versions; test active-version registry mismatch rejection with `UNSUPPORTED_SCHEMA` or stale-version outcome. Verify no silent regeneration.

## Learning Outputs
Two independent runs must produce byte-identical outputs and identical hashes. Retain a machine-readable fixture artifact and verify command exit 0, zero failed assertions, and zero missing test cases.

## Next Action
Post experiment evidence to the Experiment issue with command, commit, artifact path/hash, and timestamp. Promotion remains blocked until evidence is attached and reviewed.
